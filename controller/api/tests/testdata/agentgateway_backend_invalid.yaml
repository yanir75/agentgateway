_err: deploymentName is required for this apiVersion
apiVersion: agentgateway.dev/v1alpha1
kind: AgentgatewayBackend
metadata:
  name: azure-invalid-deployment-name
spec:
  ai:
    provider:
      azureopenai:
        apiVersion: 2024-01-01
        endpoint: test
---
_err: unknown fields
apiVersion: agentgateway.dev/v1alpha1
kind: AgentgatewayBackend
metadata:
  name: prompt-guard-auxiliary-oauth-token-exchange
spec:
  ai:
    provider:
      openai: {}
    policies:
      ai:
        promptGuard:
          request:
          - openAIModeration:
              policies:
                auth:
                  oauthTokenExchange: {}
---
_err: AI policies require AI
apiVersion: agentgateway.dev/v1alpha1
kind: AgentgatewayBackend
metadata:
  name: ai-pol-without-ai
spec:
  static:
    host: example.com
    port: 80
  policies:
    ai:
      defaults:
        - field: hi
          value: 1
---
_err: MCP policies require MCP
apiVersion: agentgateway.dev/v1alpha1
kind: AgentgatewayBackend
metadata:
  name: mcp-pol-without-mcp
spec:
  static:
    host: example.com
    port: 80
  policies:
    mcp:
      authorization:
        policy:
          matchExpressions:
          - 'true'
---
_err: unknown fields
apiVersion: agentgateway.dev/v1alpha1
kind: AgentgatewayBackend
metadata:
  name: mcp-static-target-authentication
spec:
  mcp:
    targets:
    - name: target
      static:
        host: example.com
        port: 443
        policies:
          mcp:
            authentication:
              issuer: https://issuer.example.com/
              jwks:
                backendRef:
                  name: jwks
                  port: 443
                jwksPath: /.well-known/jwks.json
---
_err: path and pathPrefix are mutually exclusive
apiVersion: agentgateway.dev/v1alpha1
kind: AgentgatewayBackend
metadata:
  name: path-and-pathprefix
spec:
  ai:
    provider:
      openai: {}
      path: /custom/path
      pathPrefix: /custom
---
_err: pathPrefix requires host to be set
apiVersion: agentgateway.dev/v1alpha1
kind: AgentgatewayBackend
metadata:
  name: pathprefix-without-host
spec:
  ai:
    provider:
      openai: {}
      pathPrefix: /custom
---
_err: both host and port must be set together
apiVersion: agentgateway.dev/v1alpha1
kind: AgentgatewayBackend
metadata:
  name: host-without-port
spec:
  ai:
    provider:
      openai: {}
      host: example.com
---
_err: exactly one of the fields in [host backendRef] must be set
apiVersion: agentgateway.dev/v1alpha1
kind: AgentgatewayBackend
metadata:
  name: mcp-backend-ref-missing-port
spec:
  mcp:
    targets:
    - name: bad-ref
      static:
        host: mcp.example.com
        backendRef:
          name: mcp-service
        port: 8080
---
_err: exactly one of the fields in [host backendRef] must be set
apiVersion: agentgateway.dev/v1alpha1
kind: AgentgatewayBackend
metadata:
  name: mcp-backend-ref-no-target
spec:
  mcp:
    targets:
    - name: bad-ref
      static:
        port: 8080
---
_err: mcp target policies may not be used with backendRef
apiVersion: agentgateway.dev/v1alpha1
kind: AgentgatewayBackend
metadata:
  name: mcp-backend-ref-with-policies
spec:
  mcp:
    targets:
    - name: bad-ref
      static:
        backendRef:
          name: mcp-service
        port: 8080
        policies:
          tls: {}
---
_err: Required value
apiVersion: agentgateway.dev/v1alpha1
kind: AgentgatewayBackend
metadata:
  name: custom-provider-missing-formats
spec:
  ai:
    provider:
      custom:
        backendRef:
          name: llm-service
          port: 8080
---
_err: should have at least 1 items
apiVersion: agentgateway.dev/v1alpha1
kind: AgentgatewayBackend
metadata:
  name: custom-provider-empty-formats
spec:
  ai:
    provider:
      custom:
        backendRef:
          name: llm-service
          port: 8080
        formats: []
---
_err: Unsupported value
apiVersion: agentgateway.dev/v1alpha1
kind: AgentgatewayBackend
metadata:
  name: custom-provider-unsupported-format
spec:
  ai:
    provider:
      custom:
        backendRef:
          name: llm-service
          port: 8080
        formats:
        - type: Detect
---
_err: path, pathPrefix, and custom format paths are mutually exclusive
apiVersion: agentgateway.dev/v1alpha1
kind: AgentgatewayBackend
metadata:
  name: custom-provider-path-and-format-path
spec:
  ai:
    provider:
      custom:
        backendRef:
          name: llm-service
          port: 8080
        formats:
        - type: Messages
          path: /api/messages
      path: /v1/messages
---
_err: path, pathPrefix, and custom format paths are mutually exclusive
apiVersion: agentgateway.dev/v1alpha1
kind: AgentgatewayBackend
metadata:
  name: custom-provider-pathprefix-and-format-path
spec:
  ai:
    provider:
      custom:
        formats:
        - type: Messages
          path: /api/messages
      host: llm.example.com
      port: 443
      pathPrefix: /custom
---
_err: path must start with /
apiVersion: agentgateway.dev/v1alpha1
kind: AgentgatewayBackend
metadata:
  name: custom-provider-format-path-missing-slash
spec:
  ai:
    provider:
      custom:
        backendRef:
          name: llm-service
          port: 8080
        formats:
        - type: Messages
          path: api/messages
---
_err: custom providers must specify exactly one of backendRef or host and port
apiVersion: agentgateway.dev/v1alpha1
kind: AgentgatewayBackend
metadata:
  name: custom-provider-missing-target
spec:
  ai:
    provider:
      custom:
        formats:
        - type: Completions
---
_err: custom providers must specify exactly one of backendRef or host and port
apiVersion: agentgateway.dev/v1alpha1
kind: AgentgatewayBackend
metadata:
  name: custom-provider-backendref-and-host-port
spec:
  ai:
    provider:
      custom:
        backendRef:
          name: llm-service
          port: 8080
        formats:
        - type: Completions
      host: llm.example.com
      port: 443
---
_err: custom provider backendRef may target only Service or InferencePool
apiVersion: agentgateway.dev/v1alpha1
kind: AgentgatewayBackend
metadata:
  name: custom-provider-unsupported-backendref-kind
spec:
  ai:
    provider:
      custom:
        backendRef:
          group: agentgateway.dev
          kind: AgentgatewayBackend
          name: other-backend
        formats:
        - type: Completions

---
_err: Must have port for Service reference
apiVersion: agentgateway.dev/v1alpha1
kind: AgentgatewayBackend
metadata:
  name: custom-provider-default-service-missing-port
spec:
  ai:
    provider:
      custom:
        backendRef:
          name: llm-service
        formats:
        - type: Completions
---
_err: Bedrock guardrails cannot be used with MantlePreferred or MantleOnly
apiVersion: agentgateway.dev/v1alpha1
kind: AgentgatewayBackend
metadata:
  name: bedrock-backend-mantlepreferred-guardrail
spec:
  ai:
    provider:
      bedrock:
        endpointPreference: MantlePreferred
        guardrail:
          identifier: test-guardrail
          version: "1"
---
_err: Bedrock guardrails cannot be used with MantlePreferred or MantleOnly
apiVersion: agentgateway.dev/v1alpha1
kind: AgentgatewayBackend
metadata:
  name: bedrock-group-mantleonly-guardrail
spec:
  ai:
    groups:
    - providers:
      - name: bedrock
        bedrock:
          endpointPreference: MantleOnly
          guardrail:
            identifier: test-guardrail
            version: "1"
